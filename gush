#!/bin/bash

git add .
git commit -m 'yes'
git push

source ~/bin/bashColors

sizeOfDir=$( echo `du -sh` | cut -d " " -f1 )
printf "${CYAN}Size of dir (including ignored): ${bold}$sizeOfDir\n${normal}"
printf "${ORANGE}$(date)${NC}\n"
