#!/bin/bash

git add .
git commit -m 'yes'
git push

source ~/.bashColors

sizeOfDir=$( echo `du -sh` | cut -d " " -f1 )
printf "${BLUE}Size of dir (including ignored): ${BLUE}${BOLD}$sizeOfDir\n${NORMALT}"
printf "${ORANGE}$(date)${NC}\n"
